# =======================================
# Configuration for integration test
#
# - dedicated application-${profileName}.yml because application.yml is excluded from *-tests.jar file
# - use it when needed with profile name in spring.profiles.active.
# =======================================

# LOG
logging.level.root: INFO
logging.level.org.springframework.web: DEBUG
logging.level.com.oryam.maven: TRACE
logging.level.org.hibernate.SQL: DEBUG
logging.level.org.hibernate.type: TRACE
#spring.jpa.properties.hibernate.format_sql: true
